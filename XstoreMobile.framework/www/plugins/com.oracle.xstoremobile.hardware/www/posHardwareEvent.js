cordova.define("com.oracle.xstoremobile.hardware.posHardwareEvent", function(require, exports, module) {

/*
 * tsmith
 *
*/

var posHardwareEvent = function(eventId, map) {
    this.eventId = eventId;
    this.map = map;
};

module.exports = posHardwareEvent;

});
